
let maxNum = 0;

function getMaxSum(array: number[]): number {
  calcLocalMaxSum(array[0], array.slice(2))

  return maxNum;
}

function calcLocalMaxSum(sum: number, array: number[]) {
  if(array.length > 1) {

  } else {

  }
}